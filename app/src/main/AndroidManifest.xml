<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.khesya.idn.dzikirapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DzikirApp">
        <activity android:name=".detail.DzikirPetangActivity"></activity>
        <activity android:name=".detail.DzikirPagiActivity" />
        <activity android:name=".detail.DetailArtikelActivity" />
        <activity android:name=".UI.DzikirPagiPetangActivity" />
        <activity android:name=".UI.DzikirSetiapSaatActivity" />
        <activity android:name=".UI.DoaHarianActivity" />
        <activity android:name=".UI.QauliyahActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>